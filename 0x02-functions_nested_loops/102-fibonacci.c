#include <stdio.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

int main(void)
{
	int f1, f2, i;

	for (i = 1; i <= 50; i++)
	{
		printf("%d ", f2);
		printf(", ");
		int next = f1 + f2;
		f1 = f2;
		f2 = next;
	}
	printf("\n");
	return (0);
}
